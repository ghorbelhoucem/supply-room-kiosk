{
  "name": "supply-room-kiosk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "npm run test:smoke",
    "test:smoke": "playwright test",
    "build": "echo \"No build step configured\""
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "eslint": "^9.32.0",
    "globals": "^15.9.0",
    "prettier": "^3.3.3"
  }
}
